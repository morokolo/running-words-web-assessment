import { WordFilterPipePipe } from './word-filter-pipe.pipe';

describe('WordFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new WordFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
